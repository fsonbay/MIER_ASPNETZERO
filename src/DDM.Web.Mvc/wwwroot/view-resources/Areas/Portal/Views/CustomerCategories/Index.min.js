(function () {
    $(function () {

        var _$customerCategoriesTable = $('#CustomerCategoriesTable');
        var _customerCategoriesService = abp.services.app.customerCategories;
		
        $('.date-picker').datetimepicker({
            locale: abp.localization.currentLanguage.name,
            format: 'L'
        });

        var _permissions = {
            create: abp.auth.hasPermission('Pages.CustomerCategories.Create'),
            edit: abp.auth.hasPermission('Pages.CustomerCategories.Edit'),
            'delete': abp.auth.hasPermission('Pages.CustomerCategories.Delete')
        };

         var _createOrEditModal = new app.ModalManager({
                    viewUrl: abp.appPath + 'Portal/CustomerCategories/CreateOrEditModal',
                    scriptUrl: abp.appPath + 'view-resources/Areas/Portal/Views/CustomerCategories/_CreateOrEditModal.js',
                    modalClass: 'CreateOrEditCustomerCategoryModal'
                });
                   

		 var _viewCustomerCategoryModal = new app.ModalManager({
            viewUrl: abp.appPath + 'Portal/CustomerCategories/ViewcustomerCategoryModal',
            modalClass: 'ViewCustomerCategoryModal'
        });

		
		

        var getDateFilter = function (element) {
            if (element.data("DateTimePicker").date() == null) {
                return null;
            }
            return element.data("DateTimePicker").date().format("YYYY-MM-DDT00:00:00Z"); 
        }
        
        var getMaxDateFilter = function (element) {
            if (element.data("DateTimePicker").date() == null) {
                return null;
            }
            return element.data("DateTimePicker").date().format("YYYY-MM-DDT23:59:59Z"); 
        }

        var dataTable = _$customerCategoriesTable.DataTable({
            paging: true,
            serverSide: true,
            processing: true,
            listAction: {
                ajaxFunction: _customerCategoriesService.getAll,
                inputFilter: function () {
                    return {
					filter: $('#CustomerCategoriesTableFilter').val(),
					nameFilter: $('#NameFilterId').val(),
					descriptionFilter: $('#DescriptionFilterId').val()
                    };
                }
            },
            columnDefs: [
                {
                    className: 'control responsive',
                    orderable: false,
                    render: function () {
                        return '';
                    },
                    targets: 0
                },
                {
                    width: 120,
                    targets: 1,
                    data: null,
                    orderable: false,
                    autoWidth: false,
                    defaultContent: '',
                    rowAction: {
                        cssClass: 'btn btn-brand dropdown-toggle',
                        text: '<i class="fa fa-cog"></i> ' + app.localize('Actions') + ' <span class="caret"></span>',
                        items: [
						{
                                text: app.localize('View'),
                                iconStyle: 'far fa-eye mr-2',
                                action: function (data) {
                                    _viewCustomerCategoryModal.open({ id: data.record.customerCategory.id });
                                }
                        },
						{
                            text: app.localize('Edit'),
                            iconStyle: 'far fa-edit mr-2',
                            visible: function () {
                                return _permissions.edit;
                            },
                            action: function (data) {
                            _createOrEditModal.open({ id: data.record.customerCategory.id });                                
                            }
                        }, 
						{
                            text: app.localize('Delete'),
                            iconStyle: 'far fa-trash-alt mr-2',
                            visible: function () {
                                return _permissions.delete;
                            },
                            action: function (data) {
                                deleteCustomerCategory(data.record.customerCategory);
                            }
                        }]
                    }
                },
					{
						targets: 2,
						 data: "customerCategory.name",
						 name: "name"   
					},
					{
						targets: 3,
						 data: "customerCategory.description",
						 name: "description"   
					}
            ]
        });

        function getCustomerCategories() {
            dataTable.ajax.reload();
        }

        function deleteCustomerCategory(customerCategory) {
            abp.message.confirm(
                '',
                app.localize('AreYouSure'),
                function (isConfirmed) {
                    if (isConfirmed) {
                        _customerCategoriesService.delete({
                            id: customerCategory.id
                        }).done(function () {
                            getCustomerCategories(true);
                            abp.notify.success(app.localize('SuccessfullyDeleted'));
                        });
                    }
                }
            );
        }

		$('#ShowAdvancedFiltersSpan').click(function () {
            $('#ShowAdvancedFiltersSpan').hide();
            $('#HideAdvancedFiltersSpan').show();
            $('#AdvacedAuditFiltersArea').slideDown();
        });

        $('#HideAdvancedFiltersSpan').click(function () {
            $('#HideAdvancedFiltersSpan').hide();
            $('#ShowAdvancedFiltersSpan').show();
            $('#AdvacedAuditFiltersArea').slideUp();
        });

        $('#CreateNewCustomerCategoryButton').click(function () {
            _createOrEditModal.open();
        });        

		

        abp.event.on('app.createOrEditCustomerCategoryModalSaved', function () {
            getCustomerCategories();
        });

		$('#GetCustomerCategoriesButton').click(function (e) {
            e.preventDefault();
            getCustomerCategories();
        });

		$(document).keypress(function(e) {
		  if(e.which === 13) {
			getCustomerCategories();
		  }
		});
		
		
		
    });
})();
